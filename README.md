# RVC App - Student Attendance & Activity Management System

ระบบจัดการกิจกรรมและเช็คชื่อนักเรียน ประกอบด้วยแอปพลิเคชัน React Native สำหรับนักเรียนและหน้าเว็บ Next.js สำหรับแอดมิน

## 📋 สารบัญ

- [คุณสมบัติหลัก](#คุณสมบัติหลัก)
- [สถาปัตยกรรมระบบ](#สถาปัตยกรรมระบบ)
- [บทบาทผู้ใช้ (Roles)](#บทบาทผู้ใช้-roles)
- [การติดตั้งและตั้งค่า](#การติดตั้งและตั้งค่า)
- [การตั้งค่าฐานข้อมูล](#การตั้งค่าฐานข้อมูล)
- [การรันแอปพลิเคชัน](#การรันแอปพลิเคชัน)
- [การใช้งาน](#การใช้งาน)
- [การทดสอบ](#การทดสอบ)
- [แก้ไขปัญหา](#แก้ไขปัญหา)

## 🎯 คุณสมบัติหลัก

### สำหรับนักเรียน (Mobile App)
- ✅ เข้าสู่ระบบด้วยรหัสนักศึกษา หรืออีเมล
- ✅ ลงทะเบียนบัญชีใหม่
- ✅ เช็คชื่อเข้าร่วมกิจกรรมด้วยการถ่ายรูป
- ✅ ดูแดชบอร์ดสรุปกิจกรรมและการเข้าร่วม
- ✅ ดูข่าวสารและประกาศ
- ✅ จัดการโปรไฟล์ส่วนตัว

### สำหรับแอดมิน/เจ้าหน้าที่ (Web Admin Panel)
- ✅ แดชบอร์ดสรุปสถิติ
- ✅ จัดการนักเรียน (เพิ่ม, แก้ไข, ค้นหา)
- ✅ จัดการกิจกรรม (สร้าง, แก้ไข, ดูรายละเอียด)
- ✅ ดูรายงานการเข้าร่วมกิจกรรม (Attendance Records)
- ✅ ดูการเข้าร่วมแบบเรียลไทม์ (Live Activity)
- ✅ จัดการประกาศและข่าวสาร
- ✅ จัดการแผนกวิชาและห้องเรียน
- ✅ ดูรายงานและวิเคราะห์ข้อมูล (Analytics)

## 🏗️ สถาปัตยกรรมระบบ

```
┌─────────────────┐         ┌──────────────┐         ┌─────────────┐
│  React Native   │◄───────►│   Supabase   │◄───────►│ PostgreSQL  │
│     Mobile      │         │   Backend    │         │  Database   │
│      App        │         │   (Auth +    │         │             │
└─────────────────┘         │    API)      │         └─────────────┘
                            └──────────────┘
                                     ▲
                                     │
                            ┌────────┴────────┐
                            │                 │
                     ┌──────┴──────┐  ┌──────┴──────┐
                     │  Next.js    │  │   Storage   │
                     │  Admin      │  │   (Photos)  │
                     │   Panel     │  │             │
                     └─────────────┘  └─────────────┘
```

### เทคโนโลยีที่ใช้

**Mobile App:**
- React Native (Expo)
- TypeScript
- React Navigation
- Supabase Client

**Admin Panel:**
- Next.js 14 (App Router)
- TypeScript
- Tailwind CSS
- Supabase Client
- React Hook Form
- Chart.js

**Backend:**
- Supabase (PostgreSQL + Auth + Storage + Realtime)
- Row Level Security (RLS)

## 👥 บทบาทผู้ใช้ (Roles)

### 1. Student (นักเรียน)
- เข้าสู่ระบบด้วยรหัสนักศึกษาหรืออีเมล
- เช็คชื่อเข้าร่วมกิจกรรม
- ดูกิจกรรมและข่าวสาร
- จัดการโปรไฟล์ส่วนตัว

### 2. Staff (เจ้าหน้าที่)
- เข้าถึงหน้าแอดมิน
- จัดการกิจกรรม
- ดูรายงานการเข้าร่วม
- จัดการประกาศ

### 3. Admin (ผู้ดูแลระบบ)
- เข้าถึงฟีเจอร์ทั้งหมดของ Staff
- จัดการนักเรียน
- จัดการแผนกวิชาและห้องเรียน
- ดูรายงานและวิเคราะห์ข้อมูล
- จัดการผู้ใช้งานทั้งหมด

## 🚀 การติดตั้งและตั้งค่า

### ความต้องการของระบบ
- Node.js 18+ 
- npm หรือ yarn
- Expo CLI
- บัญชี Supabase

### 1. Clone โปรเจกต์

```bash
git clone <repository-url>
cd Test-rvc-app
```

### 2. ติดตั้ง Dependencies

#### สำหรับ Mobile App
```bash
npm install
# หรือ
yarn install
```

#### สำหรับ Admin Panel
```bash
cd admin-panel
npm install
# หรือ
yarn install
cd ..
```

### 3. ตั้งค่า Environment Variables

#### สำหรับ Mobile App
สร้างไฟล์ `.env` ในโฟลเดอร์ root:

```env
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

#### สำหรับ Admin Panel
สร้างไฟล์ `.env.local` ในโฟลเดอร์ `admin-panel/`:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

**วิธีหาค่า Environment Variables:**
1. ไปที่ [Supabase Dashboard](https://app.supabase.com)
2. เลือกโปรเจกต์ของคุณ
3. ไปที่ Settings > API
4. Copy `Project URL` และ `anon public` key

## 🗄️ การตั้งค่าฐานข้อมูล

### 1. สร้างโปรเจกต์ Supabase

1. ไปที่ [Supabase](https://supabase.com) และสร้างบัญชี
2. สร้างโปรเจกต์ใหม่
3. รอให้ฐานข้อมูลพร้อมใช้งาน

### 2. รัน Schema SQL

1. ไปที่ SQL Editor ใน Supabase Dashboard
2. เปิดไฟล์ `database/schema.sql`
3. คัดลอกและรัน SQL ทั้งหมด

ไฟล์นี้จะสร้าง:
- ตารางทั้งหมด (profiles, activities, attendance_records, announcements, departments, classrooms)
- Indexes สำหรับประสิทธิภาพ
- Row Level Security (RLS) policies
- Triggers สำหรับอัปเดต timestamp
- ข้อมูลตัวอย่าง (departments, classrooms)

### 3. ตั้งค่า Storage (สำหรับรูปภาพ)

1. ไปที่ Storage ใน Supabase Dashboard
2. สร้าง bucket ชื่อ `attendance-photos`
3. ตั้งค่าเป็น Public bucket
4. ตั้งค่า Policies:
   - ให้ผู้ใช้ authenticated อัปโหลดได้
   - ให้ทุกคนอ่านได้ (public read)

### 4. สร้างผู้ใช้แอดมิน

หลังจากรัน schema แล้ว ให้สร้างผู้ใช้แอดมิน:

1. ไปที่ Authentication > Users
2. สร้างผู้ใช้ใหม่ด้วยอีเมลและรหัสผ่าน
3. ไปที่ SQL Editor และรันคำสั่งนี้ (แทนที่ `user_email@example.com` ด้วยอีเมลที่สร้าง):

```sql
-- อัปเดต role เป็น admin
UPDATE profiles
SET role = 'admin'
WHERE email = 'user_email@example.com';
```

หรือสร้างผ่าน SQL โดยตรง:

```sql
-- สร้างผู้ใช้ใน auth.users (ต้องใช้ Supabase Admin API หรือผ่าน Dashboard)
-- จากนั้นสร้าง profile
INSERT INTO profiles (id, email, first_name, last_name, role)
VALUES (
  'user_id_from_auth_users',
  'admin@example.com',
  'Admin',
  'User',
  'admin'
);
```

## 🏃 การรันแอปพลิเคชัน

### รัน Mobile App

#### Development Mode
```bash
npm start
# หรือ
yarn start
```

จากนั้นเลือก:
- `a` สำหรับ Android
- `i` สำหรับ iOS
- `w` สำหรับ Web

#### Web Platform (แก้ปัญหา Metro Bundler)
```bash
npm run web
# หรือ
expo start --web
```

**หมายเหตุ:** หากพบปัญหา 500 error หรือ MIME type error:
1. ตรวจสอบว่าไฟล์ `metro.config.js` และ `babel.config.js` ถูกสร้างแล้ว
2. ลบ cache: `expo start -c`
3. ตรวจสอบ environment variables

#### Android
```bash
npm run android
```

#### iOS
```bash
npm run ios
```

### รัน Admin Panel

#### Development Mode
```bash
cd admin-panel
npm run dev
# หรือ
yarn dev
```

เปิดเบราว์เซอร์ที่ `http://localhost:3000`

#### Production Build
```bash
cd admin-panel
npm run build
npm start
```

## 📱 การใช้งาน

### สำหรับนักเรียน

#### 1. ลงทะเบียน
1. เปิดแอป
2. กด "ยังไม่มีบัญชี? ลงทะเบียนที่นี่"
3. กรอกข้อมูล:
   - รหัสนักศึกษา
   - บัตรประชาชน
   - ชื่อ-นามสกุล
   - อีเมล
   - รหัสผ่าน
   - แผนกวิชา
   - ห้องเรียน
   - ระดับชั้น
4. กด "ลงทะเบียน"
5. ตรวจสอบอีเมลเพื่อยืนยันบัญชี

#### 2. เข้าสู่ระบบ
1. เปิดแอป
2. เลือก "นักเรียน" หรือ "อีเมล"
3. กรอกรหัสนักศึกษาหรืออีเมล
4. กรอกรหัสผ่าน
5. กด "เข้าสู่ระบบ"

#### 3. เช็คชื่อกิจกรรม
1. ไปที่หน้า "เช็คชื่อกิจกรรม"
2. เลือกกิจกรรมที่ต้องการ
3. กด "เช็คชื่อ"
4. ถ่ายรูป (ถ้าจำเป็น)
5. กด "ยืนยัน"

#### 4. ดูแดชบอร์ด
- ดูกิจกรรมที่กำลังเปิด
- ดูสถิติการเข้าร่วม
- ดูประวัติการเช็คชื่อ

#### 5. ดูข่าวสาร
- ดูประกาศและข่าวสารทั้งหมด
- กรองตามแผนกวิชาและระดับชั้น

### สำหรับแอดมิน/เจ้าหน้าที่

#### 1. เข้าสู่ระบบ
1. เปิด `http://localhost:3000` (หรือ URL ของ production)
2. กรอกอีเมลและรหัสผ่าน
3. กด "เข้าสู่ระบบ"

**หมายเหตุ:** เฉพาะผู้ใช้ที่มี role `admin` หรือ `staff` เท่านั้นที่สามารถเข้าถึงได้

#### 2. แดชบอร์ด
- ดูสถิติสรุป:
  - จำนวนนักเรียนทั้งหมด
  - กิจกรรมที่กำลังเปิด
  - การเข้าร่วมวันนี้
  - กิจกรรมทั้งหมด

#### 3. จัดการนักเรียน
- ดูรายชื่อนักเรียนทั้งหมด
- ค้นหานักเรียน
- กรองตามแผนกวิชาและระดับชั้น
- แก้ไขข้อมูลนักเรียน
- ดูประวัติการเข้าร่วม

#### 4. จัดการกิจกรรม
- สร้างกิจกรรมใหม่:
  - ชื่อกิจกรรม
  - รายละเอียด
  - ประเภทกิจกรรม
  - สถานที่
  - วันที่และเวลาเริ่มต้น-สิ้นสุด
  - กำหนดกลุ่มเป้าหมาย (แผนก, ห้องเรียน, ระดับชั้น)
  - กำหนดว่าต้องการรูปถ่ายหรือไม่
- แก้ไขกิจกรรม
- ดูรายละเอียดกิจกรรม
- ดูการเข้าร่วมแบบเรียลไทม์ (Live Activity)

#### 5. ดูรายงานการเข้าร่วม
- ดูรายการเช็คชื่อทั้งหมด
- ค้นหาและกรองตาม:
  - ชื่อนักเรียน
  - กิจกรรม
  - วันที่
  - แผนกวิชา
  - ห้องเรียน
  - ระดับชั้น
- แก้ไขหรือลบรายการเช็คชื่อ

#### 6. จัดการประกาศ
- สร้างประกาศใหม่
- แก้ไขประกาศ
- เผยแพร่หรือยกเลิกการเผยแพร่
- กำหนดกลุ่มเป้าหมาย

#### 7. วิเคราะห์ข้อมูล (Analytics)
- ดูสถิติการเข้าร่วมตามแผนกวิชา
- ดูสถิติการเข้าร่วมตามห้องเรียน
- ดูกราฟและรายงานต่างๆ

## 🧪 การทดสอบ

### ทดสอบ Mobile App

#### 1. ทดสอบการเข้าสู่ระบบ
- [ ] เข้าสู่ระบบด้วยรหัสนักศึกษา
- [ ] เข้าสู่ระบบด้วยอีเมล
- [ ] ทดสอบรหัสผ่านผิด
- [ ] ทดสอบการจำรหัสผ่าน

#### 2. ทดสอบการลงทะเบียน
- [ ] ลงทะเบียนด้วยข้อมูลที่ถูกต้อง
- [ ] ทดสอบรหัสนักศึกษาซ้ำ
- [ ] ทดสอบอีเมลซ้ำ
- [ ] ทดสอบการยืนยันอีเมล

#### 3. ทดสอบการเช็คชื่อ
- [ ] เช็คชื่อกิจกรรม
- [ ] ถ่ายรูปประกอบการเช็คชื่อ
- [ ] ทดสอบเช็คชื่อซ้ำ
- [ ] ทดสอบเช็คชื่อก่อนเวลาเริ่มกิจกรรม
- [ ] ทดสอบเช็คชื่อหลังเวลาสิ้นสุดกิจกรรม

#### 4. ทดสอบฟีเจอร์อื่นๆ
- [ ] ดูแดชบอร์ด
- [ ] ดูข่าวสาร
- [ ] จัดการโปรไฟล์
- [ ] อัปเดตรูปโปรไฟล์

### ทดสอบ Admin Panel

#### 1. ทดสอบการเข้าสู่ระบบ
- [ ] เข้าสู่ระบบด้วยอีเมลแอดมิน
- [ ] เข้าสู่ระบบด้วยอีเมล staff
- [ ] ทดสอบการเข้าถึงด้วย role student (ควรถูกปฏิเสธ)
- [ ] ทดสอบการออกจากระบบ

#### 2. ทดสอบการจัดการนักเรียน
- [ ] ดูรายชื่อนักเรียนทั้งหมด
- [ ] ค้นหานักเรียน
- [ ] กรองตามแผนกวิชา
- [ ] แก้ไขข้อมูลนักเรียน

#### 3. ทดสอบการจัดการกิจกรรม
- [ ] สร้างกิจกรรมใหม่
- [ ] แก้ไขกิจกรรม
- [ ] ดูรายละเอียดกิจกรรม
- [ ] ดูการเข้าร่วมแบบเรียลไทม์
- [ ] เปลี่ยนสถานะกิจกรรม

#### 4. ทดสอบการดูรายงาน
- [ ] ดูรายงานการเข้าร่วมทั้งหมด
- [ ] ค้นหาและกรองรายงาน
- [ ] แก้ไขรายการเช็คชื่อ
- [ ] ลบรายการเช็คชื่อ

#### 5. ทดสอบฟีเจอร์อื่นๆ
- [ ] จัดการประกาศ
- [ ] ดู Analytics
- [ ] จัดการแผนกวิชา
- [ ] จัดการห้องเรียน

### ทดสอบฐานข้อมูล

#### 1. ทดสอบ Row Level Security (RLS)
- [ ] นักเรียนสามารถดูโปรไฟล์ของตัวเองได้
- [ ] นักเรียนไม่สามารถดูโปรไฟล์ของคนอื่นได้
- [ ] แอดมินสามารถดูโปรไฟล์ทั้งหมดได้
- [ ] นักเรียนสามารถสร้าง attendance record ของตัวเองได้
- [ ] นักเรียนไม่สามารถสร้าง attendance record ของคนอื่นได้

#### 2. ทดสอบ Relationships
- [ ] Foreign keys ทำงานถูกต้อง
- [ ] Cascade deletes ทำงานถูกต้อง
- [ ] Joins ทำงานถูกต้อง

## 🔧 แก้ไขปัญหา

### ปัญหา Metro Bundler (500 error, MIME type error)

**อาการ:**
```
Failed to load resource: the server responded with a status of 500 (Internal Server Error)
Refused to execute script because its MIME type ('application/json') is not executable
```

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์ `metro.config.js` และ `babel.config.js` มีอยู่
2. ลบ cache:
   ```bash
   expo start -c
   ```
3. ลบโฟลเดอร์ `.expo` และ `node_modules`:
   ```bash
   rm -rf .expo node_modules
   npm install
   ```
4. ตรวจสอบ environment variables
5. ใช้ `expo start --web` แทนการเปิดผ่านเบราว์เซอร์โดยตรง

### ปัญหาการเชื่อมต่อ Supabase

**อาการ:** ไม่สามารถเชื่อมต่อฐานข้อมูลได้

**วิธีแก้:**
1. ตรวจสอบ environment variables:
   - ตรวจสอบว่า `EXPO_PUBLIC_SUPABASE_URL` และ `EXPO_PUBLIC_SUPABASE_ANON_KEY` ถูกตั้งค่าแล้ว
   - ตรวจสอบว่าไม่มีช่องว่างหรืออักขระพิเศษ
2. ตรวจสอบ Supabase Dashboard:
   - ตรวจสอบว่าโปรเจกต์ยังทำงานอยู่
   - ตรวจสอบ API keys ว่าถูกต้อง
3. ตรวจสอบ Network:
   - ตรวจสอบว่าไม่มี firewall หรือ proxy บล็อก
   - ตรวจสอบ CORS settings ใน Supabase

### ปัญหาการเข้าสู่ระบบไม่ได้

**วิธีแก้:**
1. ตรวจสอบว่า user มี profile ในตาราง `profiles`:
   ```sql
   SELECT * FROM profiles WHERE email = 'user@example.com';
   ```
2. ตรวจสอบว่า role ถูกตั้งค่าถูกต้อง:
   ```sql
   SELECT role FROM profiles WHERE email = 'user@example.com';
   ```
3. ตรวจสอบว่า email ถูกยืนยันแล้ว (Supabase Auth)
4. ตรวจสอบ RLS policies

### ปัญหา Realtime ไม่ทำงาน

**วิธีแก้:**
1. ตรวจสอบว่า Realtime เปิดใช้งานใน Supabase:
   - ไปที่ Database > Replication
   - เปิดใช้งานสำหรับตารางที่ต้องการ
2. ตรวจสอบการเชื่อมต่อ WebSocket
3. ตรวจสอบว่า subscription ถูกสร้างและ subscribe แล้ว

### ปัญหา Storage (อัปโหลดรูปไม่ได้)

**วิธีแก้:**
1. ตรวจสอบว่า bucket ถูกสร้างแล้ว
2. ตรวจสอบ Policies:
   - ให้ authenticated users อัปโหลดได้
   - ให้ทุกคนอ่านได้ (ถ้าต้องการ)
3. ตรวจสอบ file size limits
4. ตรวจสอบ file types ที่อนุญาต

## 📝 หมายเหตุสำคัญ

1. **Environment Variables:** อย่าลืมตั้งค่า environment variables สำหรับทั้ง mobile app และ admin panel
2. **Database Schema:** ต้องรัน `database/schema.sql` ก่อนใช้งาน
3. **RLS Policies:** ระบบใช้ Row Level Security เพื่อความปลอดภัย ตรวจสอบ policies ให้แน่ใจว่าถูกต้อง
4. **Storage:** ต้องสร้าง bucket สำหรับเก็บรูปภาพ
5. **Admin User:** ต้องสร้างผู้ใช้แอดมินและตั้ง role เป็น `admin` ผ่าน SQL

## 🆘 สนับสนุน

หากพบปัญหาหรือมีคำถาม:
1. ตรวจสอบไฟล์นี้ก่อน
2. ตรวจสอบ Supabase Documentation
3. ตรวจสอบ Expo Documentation
4. ตรวจสอบ Next.js Documentation

## 📄 License

MIT License

---

**พัฒนาโดย:** RVC Development Team  
**เวอร์ชัน:** 1.0.0  
**อัปเดตล่าสุด:** 2024
